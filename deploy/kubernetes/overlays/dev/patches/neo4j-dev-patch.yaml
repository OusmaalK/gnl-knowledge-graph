# ============================================================================
# Patch Neo4j - Développement
# ============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo4j
  namespace: gnl-knowledge-graph
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: neo4j
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1"
        env:
        - name: NEO4J_dbms_memory_heap_initial__size
          value: "256m"
        - name: NEO4J_dbms_memory_heap_max__size
          value: "1g"
        - name: NEO4J_dbms_memory_pagecache_size
          value: "512m"