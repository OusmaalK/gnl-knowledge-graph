# ============================================================================
# Patch API - Staging
# ============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: gnl-api
  namespace: gnl-knowledge-graph
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: gnl-api
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        env:
        - name: DEBUG
          value: "false"
        - name: LOG_LEVEL
          value: "INFO"